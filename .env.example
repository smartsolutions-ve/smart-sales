# ── Django ────────────────────────────────────────────────────────────────────
SECRET_KEY=CAMBIA_ESTO_genera_con_python_-c_import_secrets_print_secrets.token_urlsafe_50
DEBUG=False
ALLOWED_HOSTS=tudominio.com,www.tudominio.com,TU_IP_PUBLICA

# ── Base de datos ─────────────────────────────────────────────────────────────
# Para producción (Docker): el host "db" es el nombre del servicio en docker-compose
DATABASE_URL=postgresql://smartsales:CAMBIA_ESTO_contraseña_segura@db:5432/smartsales

# Variables separadas para docker-compose (generan la BD)
DB_NAME=smartsales
DB_USER=smartsales
DB_PASSWORD=CAMBIA_ESTO_contraseña_segura

# ── Email (Resend) ────────────────────────────────────────────────────────────
RESEND_API_KEY=re_tu_api_key_de_resend
DEFAULT_FROM_EMAIL=noreply@tudominio.com

# ── Django settings module ────────────────────────────────────────────────────
DJANGO_SETTINGS_MODULE=config.settings.prod
